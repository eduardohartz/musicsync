services:
  musicsync:
    build: .
    image: musicsync:latest
    container_name: musicsync
    restart: unless-stopped
    init: true
    env_file: .env
    ports:
      # Web panel (setup wizard, dashboard, OAuth callbacks).
      - "${PORT:-8080}:${PORT:-8080}"
    volumes:
      - ./config:/config
