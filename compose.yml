services:
  musicsync:
    image: ghcr.io/eduardohartz/musicsync:latest
    container_name: musicsync
    restart: unless-stopped
    init: true
    env_file: .env
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    volumes:
      - ./config:/config
